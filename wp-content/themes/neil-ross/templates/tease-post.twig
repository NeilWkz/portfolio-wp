{% extends "tease.twig" %}

{% block content %}
    {% if post.thumbnail.src %}
        <img class="post-thumbnail" loading="lazy" src="{{post.thumbnail.src}}" width="{{post.thumbnail.width}}" height="{{post.thumbnail.height}}"/>
        <div class="category-wrap">
        {%for category in post.categories %}
            {%if category != 'Blog' %}
                <h5 class="category">{{category}}</h5>
            {% endif %}
        {% endfor %}
        </div>
    {% endif %}
    <div class="card-body">
        <h2 class="h2">
            <a href="{{post.link}}">{{post.title}}</a>
        </h2>
        <p>{{post.preview.length(25)}}</p>

    </div>

{% endblock %}
