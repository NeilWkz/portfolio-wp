# The Wordpress Theme for neilross.dev

Built using timber, this is actually some old code I ported from my static site into a wordpress theme

## To start

`npm run watch`

## To build

`npm run build`